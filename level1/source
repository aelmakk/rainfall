#include <unistd.h>
#include <stdio.h>

void    run(void)
{
    fwrite("Good... Wait what?\n", 0x1, 0x13, stdout);
    system("/bin/sh");
}

int main(int argc, char *argv[])
{
    char buff[64];

    gets(buff);
    return (0);
}